; PlatformIO Project Configuration File
; Target: Arduino Pro Mini (ATmega328P, 5V, 16 MHz)
; For the 3.3 V / 8 MHz variant replace the env name and board value with:
;   [env:pro8MHzatmega328]
;   board = pro8MHzatmega328

[env:pro16MHzatmega328]
platform  = atmelavr
board     = pro16MHzatmega328
framework = arduino

; The Servo library ships with the Arduino AVR framework.
; Listing it explicitly makes the dependency visible and reproducible.
lib_deps =
    arduino-libraries/Servo
